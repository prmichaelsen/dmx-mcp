# OLA connection settings
OLA_HOST=localhost
OLA_PORT=9090

# To use the emulator instead of real OLA:
# 1. Run: npm run emulator
# 2. Open http://localhost:9090/ to see the DMX monitor
# 3. Start the MCP server normally — it will connect to the emulator
